venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets / local-only config — never commit
config/config.json
config/sessions/

# database/app.db and downloads/ ARE committed intentionally: the GitHub Actions
# scan workflow persists scraped state by committing it back to the repo.
database/*.db-journal
logs/*.log
!logs/.gitkeep

# OS / editor
.vscode/
.idea/
Thumbs.db
.DS_Store
